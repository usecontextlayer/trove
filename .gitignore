node_modules/
packages/*/node_modules/
.turbo/
dist/
dist-types/
*.tsbuildinfo

# wrangler's local state: the miniflare sandbox, the local D1 sqlite file, and
# the temporary-account credential file (which holds a plaintext API token).
.wrangler/
.dev.vars

# Built copy of trove-embed's dist/trove.js, staged into the registry's assets
# dir by its build script.
packages/trove-registry/public/trove.js

.DS_Store

.codegraph/
.doublecheck/
.superpowers/
.playwright-cli/
.playwright-mcp/
.claude/worktrees/
